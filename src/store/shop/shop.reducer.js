import SHOP_INITIAL_STATE from "./shop.initial-state";

const shopReducer = (state = SHOP_INITIAL_STATE, { type }) => {
  switch (type) {
    default:
      return state;
  }
};

export default shopReducer;
